config = {
    database: {
        //Server DB
        name: //Server Address Removed For Config Purposes
    },
    application: {
        secret: 'secret',
        env: 'development'
    }
}

module.exports = config;
